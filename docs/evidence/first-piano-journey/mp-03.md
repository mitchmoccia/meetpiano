# Evidence — MP-03

| Field | Value |
| --- | --- |
| Slice ID | MP-03 |
| Learner outcome | A beginner can open First Notes, explore the keyboard, find C, play neighboring C–D–E, and complete Little Wave. The next activity unlocks only when this device is ready. Help and replay do not erase device-local progress. |
| Scope changes | First Notes unit hub at `/learn`. Playable L02–L04 with explain→demo→guided→independent→transfer/review/result. Unlock gates, fading hints, adult-observed fingering/register labels, original Little Wave phrases. Marketing playground unchanged in purpose. L01 remains at `/learn/?lesson=L01`. No Next.js rewrite. Branch `cursor/setup-cloud-agent-env-d108` untouched. No Vercel / DNS changes. |
| Base commit | `f478ed60993227c04f47427c217d3b90b2e0981f` (`piany/mp-02-midi-input`) |
| Resulting commit | `72f4e5b91119619a51a309d654de0fc035487a8f` (walkthrough fixes). Product lands in `912e6bc4f697befab10df473a2fd073b4c574996`; checks in `b955648`. |
| Branch | `piany/mp-03-first-notes` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/5 (draft into `piany/mp-02-midi-input`) |
| Tests | `node --check` on touched JS passed. `node scripts/mp-01-check.mjs` passed. `node scripts/mp-02-check.mjs` passed. `node scripts/mp-03-check.mjs` passed (unlock gates, L02 doorstep + register C, L03 D–C–E not marketing C–D–E, Little Wave + cousin, later review → Retained, help/replay keep Independent). Headless Chrome walkthrough of `/`, `/learn/`, L01–L04 passed. On-screen Chrome confirmed hub locks and L01 path. |
| Preview URL / version | No production deploy. Local `python3 -m http.server 3000 --directory dist --bind 127.0.0.1`: `/` HTTP 200, `/learn/` HTTP 200, `/learn/?lesson=L01` HTTP 200. |
| Open risks | **Physical MIDI hardware is unverified.** Canonical hosting is a single Vercel project `meetpiano` plus `https://meetpiano.app` (`meetpiano-app` was deleted). Do not merge or promote production without Mitch. Not a MIDI-verified release. |
| Status | **working** (software + browser checked; hardware unchecked) |
| Next eligible slice | MP-06 — device-local progress polish |
| Next action | Continue Checkpoint A review. Do not merge this PR. Do not claim a MIDI-verified release. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Automated fixtures (2026-09-08)

`node scripts/mp-03-check.mjs`:

- First Notes catalog is L01–L04 only. `L05` does not parse.
- L02 locked until L01 Practiced. L03 locked until L02 Practiced. L04 locked until L03 Independent.
- L01 Help turns hints on and does not erase Explored / Practiced.
- L02: D remediates as the middle room; landmark C fades hints; adult other-C / other-house complete Independent; MIDI 72 counts as a new register; replay keeps Independent.
- L03: independent order is D–C–E (not C–D–E or E–D–C). Transfer is E–C–D. Fingering is adult-observed.
- L04: home phrase `C D E D E D C`, cousin `E D C D C D E`. Named later review promotes Retained.

`node scripts/mp-01-check.mjs` and `node scripts/mp-02-check.mjs` still passed.

## Browser verification (2026-09-08)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` title includes MeetPiano. Nav **First Notes**. L01 deep link present. After tapping C, marketing feedback `You’ve got this.`
- `/learn/`: device-only disclosure visible. Four cards. Fresh device: L01 open, L02–L04 locked. No L05. Invalid `?lesson=L05` stays on the hub.
- `/learn/?lesson=L01`: explain → demo → guided high/low + both clumps → Independent.
- After L01 Independent, hub unlocks L02 only.
- L02: D shows the doorstep house; C + adult register/house → Independent **C finder**.
- Help after L02 finish keeps Independent.
- L03: C–D–E guided, fingers adult-observed, D–C–E then E–C–D → Independent.
- L04: Little Wave parts, cousin listen, independent home + flipped cousin → Independent **Little Wave**.
- Final hub: L01–L04 unlocked and Independent. Marketing C–D–E keys still clickable.

On-screen Chrome (fresh profile):

- Home playground piano still plays. **First Notes** nav and L01 deep link visible.
- Hub yellow disclosure. L01 open; L02–L04 locked with unlock copy. Footer does not claim a hardware MIDI check.
- L01 `?lesson=L01` explain → see (2 and 3 labels) → try high then low.
- Direct `?lesson=L02` / `L03` / `L04` show a pink lock notice and the hub. No L05.

## Hardware check (honest)

**Unverified.** This Cloud Agent environment has no attached MIDI keyboard. No brand, USB adapter, operating system + real-device pairing, disconnect, or reconnect was exercised on hardware.

What *was* exercised:

| Check | Result |
| --- | --- |
| Browser | Google Chrome on this Linux VM |
| Web MIDI API | Present; not used with a physical instrument |
| Permission | Not granted against hardware |
| Connected inputs | None |
| Physical note-on / note-off / hold / unplug | **Not tested** |

Do not treat on-screen keys, computer keys, or simulated MIDI 72 in fixtures as a physical-keyboard pass.

## Unlock rules recorded in-product

| Activity | Unlocks when |
| --- | --- |
| L01 Meet the keyboard | Always |
| L02 Find C | L01 is Practiced or higher |
| L03 Neighbors C–D–E | L02 is Practiced or higher |
| L04 First little tune | L03 is Independent |

Help and Start over do not wipe saved evidence. Replay starts a new attempt and keeps Independent if it was already earned.
