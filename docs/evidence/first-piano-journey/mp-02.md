# Evidence — MP-02

| Field | Value |
| --- | --- |
| Slice ID | MP-02 |
| Learner outcome | A grown-up can request Web MIDI, see connected / disconnected / unsupported / denied copy, and keep using on-screen plus computer keys. Learner notes are counted once per hold. Demo audio never earns progress. Exploratory checks may accept a pitch-class in any octave; location checks can require an exact MIDI pitch. |
| Scope changes | Added `dist/js/midi.js` and `dist/js/assess.js`. Wired normalization, blur policy, device listing, and octave-aware scoring into `input.js`, `player.js`, `progress.js`, and `/learn`. Simulated fixtures in `scripts/fixtures/mp-02-midi.json` plus `scripts/mp-02-check.mjs`. Marketing playground assessment is unchanged. No Next.js rewrite. Branch `cursor/setup-cloud-agent-env-d108` untouched. No Vercel / DNS changes. |
| Base commit | `13fbd6b29f0f78278a72e25ea0fbfb65e609a1aa` (`main`, MP-00 + MP-01 merged) |
| Resulting commit | `d7a3f795e5506d7eb9f861f716031ad3417f4a23` (evidence fill). Product lands in `453fa1d`; fixtures in `e146ec9`. |
| Branch | `piany/mp-02-midi-input` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/4 (draft into `main`) |
| Tests | `node --check` on touched JS passed. `node scripts/mp-01-check.mjs` passed. `node scripts/mp-02-check.mjs` passed (held key, velocity-zero note-off, repeated notes, exact-pitch wrong octave, pitch-class any octave, demo ignored, disconnect/unsupported/no-device copy, `inputMode` + `inputDevice` stored). Headless Chrome against `http://127.0.0.1:3000` passed. On-screen Chrome walkthrough of `/learn` MIDI fallback + L01 on-screen keys passed. |
| Preview URL / version | No production deploy. Local `python3 -m http.server 3000 --directory dist`: `/` HTTP 200, `/learn` 301 → `/learn/` HTTP 200. |
| Open risks | **Physical MIDI hardware is unverified.** Chrome in this environment exposes `requestMIDIAccess` but no keyboard is attached; permission was denied or unused. Two Vercel projects still exist; this slice does not change them. Do not merge or promote production without Mitch. |
| Status | **working** (software + fixtures checked; hardware unchecked) |
| Next eligible slice | MP-03 — L02 Find C |
| Next action | Wait for Mitch review. Do not merge this PR as part of MP-02. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Automated fixtures (2026-09-08)

`node scripts/mp-02-check.mjs` against `scripts/fixtures/mp-02-midi.json`:

- Held key: second note-on while down does not score; note-off then note-on scores again.
- Velocity-zero note-on is treated as note-off.
- Exact MIDI 60 rejects heard 72 (`wrong-octave`). Pitch-class policy accepts 72 for expected 60.
- Demo source / demo-playback flag never explores and never counts.
- Attempt stores `inputMode` (`midi`, then `mixed`) and fixture device identity.
- Status copy covers unsupported, granted-with-no-device, disconnect, and connected.

`node scripts/mp-01-check.mjs` still passed (L01 loop, demo-does-not-earn, reload, first-completion).

## Browser verification (2026-09-08)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` title `MeetPiano · Little keys. Big possibilities.`; playground present. After tapping C, marketing feedback `You’ve got this.`
- `/learn/`: disclosure visible. MIDI status before click: optional stand-in copy. `navigator.requestMIDIAccess` is a function. After click: `MIDI permission was not granted. Nothing is blocked: use the on-screen keys or computer keys.`
- L01 on-screen: explain → demo skip → high 71 then low 60 → Explored. Two-black then three-black → Practiced. Stored `inputMode: touch`, `inputDevice: null`.

On-screen Chrome: `/learn/` after a blocked MIDI prompt showed **Keyboard access was not available** and the same denied copy. Guided high/low + both black-key clumps reached **Practiced**. On-screen keys stayed usable. Marketing home mini-adventure still completed C–D–E.

## Hardware check (honest)

**Unverified.** This Cloud Agent environment has no attached MIDI keyboard. No brand, USB adapter, operating system + real-device pairing, disconnect, or reconnect was exercised on hardware.

What *was* exercised:

| Check | Result |
| --- | --- |
| Browser | Google Chrome on this Linux VM |
| Web MIDI API | Present (`requestMIDIAccess` is a function) |
| Permission | Denied / not granted in the test browsers |
| Connected inputs | None |
| Physical note-on / note-off / hold / unplug | **Not tested** |

Do not treat simulated fixtures or a permission-denied click as a physical-keyboard pass.

If a later human pass uses a real instrument, record: device name, connection (USB / other), browser, OS, and whether note-on, velocity-zero note-off, hold, disconnect, and reconnect behaved as specified.

## Blur policy

Leaving the tab or window silences sound and clears on-screen pressed keys. Computer-key holds reset. MIDI notes that were already down stay locked so they cannot count again until a real note-off and a new note-on.
