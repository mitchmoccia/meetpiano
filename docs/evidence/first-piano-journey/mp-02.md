# Evidence — MP-02

| Field | Value |
| --- | --- |
| Slice ID | MP-02 |
| Learner outcome | A grown-up can request Web MIDI, see connected / disconnected / unsupported copy, and keep using on-screen plus computer keys. Learner notes are counted once per hold. Demo audio never earns progress. Exploratory checks may accept a pitch-class in any octave; location checks can require an exact MIDI pitch. |
| Scope changes | Added `dist/js/midi.js` and `dist/js/assess.js`. Wired normalization, blur policy, device listing, and octave-aware scoring into `input.js`, `player.js`, `progress.js`, and `/learn`. Simulated fixtures in `scripts/fixtures/mp-02-midi.json` plus `scripts/mp-02-check.mjs`. Marketing playground assessment is unchanged. No Next.js rewrite. Branch `cursor/setup-cloud-agent-env-d108` untouched. |
| Base commit | `13fbd6b29f0f78278a72e25ea0fbfb65e609a1aa` (`main`, MP-00 + MP-01 merged) |
| Resulting commit | *(pending this slice)* |
| Branch | `piany/mp-02-midi-input` |
| PR | *(pending draft into `main`)* |
| Tests | Pending `node --check` on touched JS, `node scripts/mp-01-check.mjs`, `node scripts/mp-02-check.mjs`, and a browser pass of `/` plus `/learn`. |
| Preview URL / version | No production deploy. |
| Open risks | No physical MIDI keyboard in this Cloud Agent environment. Hardware check is **unverified**. Two Vercel projects still exist; this slice does not change them. |
| Status | **working** |
| Next eligible slice | MP-03 — L02 Find C |
| Next action | Record automated and browser evidence. Do not merge this PR as part of MP-02. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Hardware check (honest)

**Unverified.** This environment has no attached MIDI keyboard. No brand, adapter, operating system, or browser + real-device pairing was exercised. Do not treat simulated fixtures as a physical-keyboard pass.

If a later human pass uses a real instrument, record: device name, connection (USB / other), browser, OS, and whether note-on, velocity-zero note-off, hold, disconnect, and reconnect behaved as specified.

## Blur policy

Leaving the tab or window silences sound and clears on-screen pressed keys. Computer-key holds reset. MIDI notes that were already down stay locked so they cannot count again until a real note-off and a new note-on.
