# Evidence — MP-06

| Field | Value |
| --- | --- |
| Slice ID | MP-06 |
| Learner outcome | A grown-up can see separate Practiced / Independent / Retained records on this device, get a next-session recommendation that follows actual learning, and export or import those records without inventing mastery. |
| Scope changes | Device-local skill evidence lanes, richer AttemptRecords (assistance, tempo, source, skill/curriculum versions), new-session other-pattern checks, easier work after repeated misses, export/import with version checks and repeat-safe attempt IDs, next-session card on `/learn`. Marketing playground unchanged in purpose. No Next.js rewrite. Branch `cursor/setup-cloud-agent-env-d108` untouched. No Vercel / DNS changes. Stacked on `piany/mp-05-read-and-play` @ `54d0d0c`. |
| Base commit | `54d0d0c96f2e0d2b15c0992fa5f884b49c62da87` (`piany/mp-05-read-and-play`) |
| Resulting commit | `9ee1801eac466c84b32cedced1db5a7efb92fb20` (evidence fill). Product lands in `2ed919ce9409e483162b85553cbe9792b9c095e7`; checks in `fad3d2574763bd4c3debfdfdcb401b160121a676`. |
| Branch | `piany/mp-06-adaptive-next` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/9 (draft into `piany/mp-05-read-and-play`) |
| Tests | `node --check` on touched JS passed. `node scripts/mp-01-check.mjs` through `mp-06-check.mjs` passed. Headless Chrome walkthrough of `/`, `/learn/`, seeded new-visit review, L01 result honesty, L04 cousin check, marketing C-key passed. On-screen Chrome confirmed hub recommendation, evidence lanes, export/import copy, and L04 other-pattern review. |
| Preview URL / version | No production deploy of this branch. Local `python3 -m http.server 3000 --directory dist --bind 127.0.0.1`: `/` HTTP 200, `/learn/` HTTP 200, `/learn/?lesson=L01` HTTP 200, `/learn/?lesson=L04&check=review` HTTP 200 when Independent is saved. Brand `https://meetpiano.app/learn` already HTTP 200 on current `main`. Canonical hosting is a single Vercel project `meetpiano` plus `https://meetpiano.app`. |
| Open risks | **Physical MIDI hardware is unverified.** Do not merge or promote production without Mitch. Not a MIDI-verified release. |
| Status | **working** (software + browser checked; hardware unchecked) |
| Next eligible slice | MP-07 input labels, MP-08 leftover neighborhood, or MP-11 closeout |
| Next action | Continue review. Do not merge this PR. Do not claim a MIDI-verified release. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Automated fixtures (2026-09-09)

`node scripts/mp-06-check.mjs`:

- Schema 1 stores still read and migrate to schema 2. Historical `beginner-v0` attempts stay readable and do not keep live Independent.
- L04 Independent stores skill IDs, skill version, curriculum version, assistance, and separate practiced / independent lanes. Retained stays empty until a gap.
- Touch Independent is not MIDI verified. A MIDI attempt may record heard-over-MIDI; UI copy is not a hardware certification.
- Same-session replay of L04 does not manufacture Retained.
- A new visit recommends the latest Independent-not-Retained lesson (`L04`) with `check=review` and another pattern.
- Three independent misses set easier work and leave the identical hard check.
- Export kind is `meetpiano-progress`. Import rejects a missing curriculum version, does not invent Independent, and merges repeat-safe attempt IDs.
- Invalidating `S-PHRASE` drops live L04 Independent and keeps attempts. L03 Independent stays.
- Prior unlocks stay healthy: L02 after L01 Practiced, L04 after L03 Independent, L05 after L04 Independent, L09 after L08 Independent.

`node scripts/mp-01-check.mjs`, `mp-02-check.mjs`, `mp-03-check.mjs`, `mp-04-check.mjs`, and `mp-05-check.mjs` still passed.

Product SHA: `2ed919ce9409e483162b85553cbe9792b9c095e7`. Checks SHA: `fad3d2574763bd4c3debfdfdcb401b160121a676`.

## Browser verification (2026-09-09)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` title is MeetPiano. Marketing C key still yields `You’ve got this.`
- `/learn/`: device-only disclosure names separate Practiced / Independent / Retained and says touch is never MIDI verified. Fresh device: next-session card starts Meet the keyboard. Evidence lane pills visible. Export JSON and Import JSON present. Cards L01–L12 only.
- After seeding L01–L02 Practiced and L03–L04 Independent with an older `lastSeenAt`: next-session card recommends reviewing First little tune another way.
- `/learn/?lesson=L01` after that seed: result copy includes `Touch practice. Not MIDI verified.`
- `/learn/?lesson=L04&check=review`: opens on the cousin check (`Wave the other way`), not the home explanation.

On-screen Chrome:

- Hub yellow disclosure. Next-session card and export/import section visible.
- After device-local seed: **Review First little tune another way**.
- That button opens L04 on **THE COUSIN / Wave the other way**.

## Hardware check (honest)

**Unverified.** This Cloud Agent environment has no attached MIDI keyboard. No brand, USB adapter, operating system + real-device pairing, disconnect, or reconnect was exercised on hardware.

What *was* exercised:

| Check | Result |
| --- | --- |
| Browser | Google Chrome on this Linux VM |
| Web MIDI API | Present; not used with a physical instrument |
| Permission | Not granted against hardware |
| Connected inputs | None |
| Physical note-on / note-off / hold / unplug | **Not tested** |

Do not treat on-screen keys, computer keys, or synthetic fixtures as a physical-keyboard pass.

## Evidence rules recorded in-product

| Rule | What this device does |
| --- | --- |
| Separate lanes | Explored / Practiced / Independent / Retained are stored and shown separately |
| Source honesty | Touch and computer keys cannot become MIDI verified |
| New visit | Recommends an earlier Independent skill with another pattern |
| Replay | Same-visit replay cannot manufacture Retained |
| Repeated misses | After three misses, easier work replaces the identical hard check |
| Export / import | Validates versions; merges by attempt ID; cannot invent Independent or Retained |

Help and Start over do not wipe saved evidence.
