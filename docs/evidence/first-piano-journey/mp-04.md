# Evidence — MP-04

| Field | Value |
| --- | --- |
| Slice ID | MP-04 |
| Learner outcome | After First Notes is Independent on this device, a beginner can hear a heartbeat, hold long and tap short, leave a rest, and walk C–D–E–C on the beat. Correct pitches at arbitrary times do not pass. |
| Scope changes | Rhythm Club unit (L05–L08) on `/learn`. Shared audio-clock transport and scoring. Guided vs performance windows. Synthetic fixtures. Journey map links only for First Notes and Rhythm Club. Marketing playground unchanged in purpose. No Next.js rewrite. Branch `cursor/setup-cloud-agent-env-d108` untouched. No Vercel / DNS changes. Recreated on `main` after Mitch merged MP-02+MP-03. |
| Base commit | `96860fa8262bfe2f8a902910c55745054d21eeb5` (`main`, MP-02+MP-03 merged) |
| Resulting commit | `172cecdaebc90760d254de4eafd46e38d08c590e` (evidence fill). Product lands in `93bc2c70ade7de18a0349f82dfd64940c17e51f6`; checks in `f798cd37c38fd555e23b7707c5c73f0276908920`. |
| Branch | `piany/mp-04-rhythm-club` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/6 (draft into `main`) |
| Tests | `node --check` on touched JS passed. `node scripts/mp-01-check.mjs` passed. `node scripts/mp-02-check.mjs` passed. `node scripts/mp-03-check.mjs` passed (First Notes still L01–L04; L05 locked until L04 Independent). `node scripts/mp-04-check.mjs` passed (fixtures + L05–L08 loops). Headless Chrome walkthrough of `/`, `/learn/`, unlock, L05 take, L08 lock, marketing piano passed. On-screen Chrome confirmed hub locks, unlock after L04, L05 Start the take. |
| Preview URL / version | No production deploy of this branch. Local `python3 -m http.server 3000 --directory dist`: `/` HTTP 200, `/learn/` HTTP 200, `/learn/?lesson=L05` HTTP 200 when unlocked. Brand `https://meetpiano.app/learn` already HTTP 200 (First Notes on current `main`). |
| Open risks | **Physical MIDI hardware is unverified.** Canonical hosting is a single Vercel project `meetpiano` plus `https://meetpiano.app` (`meetpiano-app` was deleted). Do not merge or promote production without Mitch. Not a MIDI-verified release. |
| Status | **working** (software + browser checked; hardware unchecked) |
| Next eligible slice | MP-06 — device-local progress polish |
| Next action | Continue Checkpoint review. Do not merge this PR. Do not claim a MIDI-verified release. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Automated fixtures (2026-09-09)

`node scripts/mp-04-check.mjs` against `scripts/fixtures/mp-04-rhythm.json` and injected fake audio clocks:

- Journey catalog is L01–L08. `L09` does not parse. First Notes cards stay L01–L04.
- L05 locked until L04 Independent. L06 after L05 Practiced. L07 after L06 Practiced. L08 after L07 Independent.
- Correct four-beat heartbeat passes. Early, late, missed, and extra taps fail.
- Count-in taps are ignored and do not fail a later correct take.
- Long hold + two shorts pass; chopping the long note is `too-short`.
- A tap in a rest is `rest` and fails. Leaving the hole empty passes.
- C–D–E–C on the beat passes. The same letters dumped as fast as possible fail.
- Pause/resume shifts the origin so remaining notes are not unfair misses.
- Disconnect aborts with `unfairFailure` and no scored miss.
- Tempo reduction (60 bpm) still accepts on-beat taps.
- Scheduler uses timers, not rAF. Pause cancels pending timers.
- L05–L08 players complete guided → independent → transfer → Independent. L08 later review → Retained. Help does not erase Independent.

`node scripts/mp-01-check.mjs`, `mp-02-check.mjs`, and `mp-03-check.mjs` still passed.

Product SHA: `93bc2c70ade7de18a0349f82dfd64940c17e51f6`. Checks SHA: `f798cd37c38fd555e23b7707c5c73f0276908920`.

## Browser verification (2026-09-09)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` title includes MeetPiano. Learn-entry includes **Rhythm Club**. Journey world 02 shows **Open Rhythm Club**. World 03 (Two-Hand Land) has no learn button.
- `/learn/`: device-only disclosure visible. Two worlds. Fresh device: First Notes L01 open; Rhythm Club locked. Cards L01–L08 only. No L09. Invalid/locked `?lesson=L05` stays on the hub.
- After seeding L04 Independent: `/learn/?unit=rhythm-club` unlocks L05.
- `/learn/?lesson=L05`: explain → hear → try. On-beat C taps scored (`With the heartbeat.`).
- `/learn/?lesson=L08` without L07 Independent stays locked.
- Marketing C key still yields `You’ve got this.`

On-screen Chrome:

- Home playground piano still plays. Journey map Rhythm Club link only for world 02.
- Hub yellow disclosure. Rhythm Club locked until Little Wave is Independent. Footer does not claim a hardware MIDI check.
- After device-local L01–L04 Independent seed: L05 Open; L06–L08 locked.
- L05 guided: Start the take, Pause, Resume, Slower heartbeat, Replay take, yellow pulse, on-screen piano. Count-in copy: **On the audio clock**.

## Hardware check (honest)

**Unverified.** This Cloud Agent environment has no attached MIDI keyboard. No brand, USB adapter, operating system + real-device pairing, disconnect, or reconnect was exercised on hardware.

What *was* exercised:

| Check | Result |
| --- | --- |
| Browser | Google Chrome on this Linux VM |
| Web MIDI API | Present; not used with a physical instrument |
| Permission | Not granted against hardware |
| Connected inputs | None |
| Physical note-on / note-off / hold / unplug | **Not tested** |

Do not treat on-screen keys, computer keys, or synthetic clock fixtures as a physical-keyboard pass.

## Unlock rules recorded in-product

| Activity | Unlocks when |
| --- | --- |
| L01–L04 First Notes | Unchanged from MP-03 |
| L05 Hear the heartbeat | L04 is Independent |
| L06 Long and short | L05 is Practiced or higher |
| L07 Silence belongs | L06 is Practiced or higher |
| L08 Notes with a beat | L07 is Independent |

Help and Start over do not wipe saved evidence. Replay take / pause / disconnect abort the current take without marking an unfair miss. Tempo reduction starts a new take at 60 bpm with the same millisecond windows.
