# Evidence — MP-01

| Field | Value |
| --- | --- |
| Slice ID | MP-01 |
| Learner outcome | A child and grown-up can open `/learn`, start L01 *Meet the keyboard*, hear notes after a gesture, explore high/low and black-key groups of two and three, and leave an on-device Explored / Practiced / Independent record. Posture is adult-observed and labeled. |
| Scope changes | Added static `/learn` (`dist/learn/`), reusable `dist/js` modules (audio, input, piano, progress, L01 spec, player), `vercel.json` rewrites for `/learn`, a minimal marketing entry to `/learn`, and this evidence. Marketing three-mission preview in `dist/app.js` is unchanged. No framework rewrite. No accounts. Branch `cursor/setup-cloud-agent-env-d108` untouched. |
| Base commit | `1cf494ffb73ca33d037b7b47ced6d74d5161d5e5` (`piany/mp-00-mission-foundation`) |
| Resulting commit | `77a58613e86e1eb846f8fe162ee604d37dafe062` (verified fill). Product lands in `a47740d`; this commit records browser evidence. |
| Branch | `piany/mp-01-learn-l01` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/3 (draft into `piany/mp-00-mission-foundation`) |
| Tests | `node --check` on `dist/app.js`, `dist/js/*.js`, `dist/js/lessons/*.js`, `dist/learn/learn.js`. `node scripts/mp-01-check.mjs` passed (corrupt/missing storage, demo-does-not-earn, reload restore, restart, first-completion once). Headless Chrome + on-screen browser walkthrough of `/` and `/learn/` passed. |
| Preview URL / version | No production deploy. Local `python3 -m http.server 3000 --directory dist`: `/` HTTP 200, `/learn` 301 → `/learn/` HTTP 200. Vercel rewrite covers `/learn` and `/learn/` → `/learn/index.html`. |
| Open risks | Python `http.server` needs the trailing slash; Vercel rewrite covers both. Canonical hosting is a single Vercel project `meetpiano` plus `https://meetpiano.app` (`meetpiano-app` was deleted). Transfer of a second two-black group needs another octave (MIDI) or a grown-up confirm because the preview piano shows one pair. Do not merge or promote production without Mitch. |
| Status | **verified** |
| Next eligible slice | MP-02 — teaching-loop engine |
| Next action | Wait for Mitch merge-or-stack / preview decision. Do not merge this PR as part of MP-01. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Browser verification (2026-09-08)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` title `MeetPiano · Little keys. Big possibilities.`; three-mission playground present; `First lesson` → `/learn` link present.
- Marketing C–D–E mission 1 still completes (`+20 XP`) and advances to mission 2.
- `/learn/`: disclosure visible; L01 explain → demo. After demo audio only, `evidenceState` stayed empty (demo cannot earn progress).
- Guided high (B) then low (C), then two-black then three-black → `practiced`. Reload restored **A grown-up check**, not explanation.
- Independent high/low + both clumps, then adult transfer confirms → result **Independent**, first-finish sticker **Keyboard explorer**.
- Reload of result hid the sticker (no duplicate first-completion reward).
- Corrupt `meetpiano:beginner-v1` (`{broken`) showed a pink notice and reset to **Not yet explored** (did not claim Independent).

On-screen Chrome walkthrough recorded the same path: marketing home → First lesson → explain → see/demo → try/guided → reload stays on posture → independent/transfer → Independent result.
