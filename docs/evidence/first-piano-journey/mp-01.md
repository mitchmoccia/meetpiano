# Evidence — MP-01

| Field | Value |
| --- | --- |
| Slice ID | MP-01 |
| Learner outcome | A child and grown-up can open `/learn`, start L01 *Meet the keyboard*, hear notes after a gesture, explore high/low and black-key groups of two and three, and leave an on-device Explored / Practiced / Independent record. Posture is adult-observed and labeled. |
| Scope changes | Added static `/learn` (`dist/learn/`), reusable `dist/js` modules (audio, input, piano, progress, L01 spec, player), `vercel.json` rewrites for `/learn`, a minimal marketing entry to `/learn`, and this evidence. Marketing three-mission preview in `dist/app.js` is unchanged. No framework rewrite. No accounts. Branch `cursor/setup-cloud-agent-env-d108` untouched. |
| Base commit | `1cf494ffb73ca33d037b7b47ced6d74d5161d5e5` (`piany/mp-00-mission-foundation`) |
| Resulting commit | _pending push_ |
| Branch | `piany/mp-01-learn-l01` |
| PR | _pending_ |
| Tests | `node --check` on JS files. `node scripts/mp-01-check.mjs` for storage validation, demo-does-not-earn, reload restore, restart, and first-completion. Browser/headless `/learn` L01 path still to record. |
| Preview URL / version | No production deploy. Preview of this branch, if created, should serve `/` (marketing) and `/learn` (L01). |
| Open risks | Python `http.server` wants `/learn/`; Vercel rewrite covers `/learn` and `/learn/`. Two Vercel projects still exist. MIDI remains optional and is not advertised as universal. Transfer of a second two-black group needs another octave (MIDI) or a grown-up confirm because the preview piano shows one pair. |
| Status | **working** |
| Next eligible slice | MP-02 — teaching-loop engine |
| Next action | Record browser verification, fill SHAs and PR URL, mark verified |
| Decision needed | Mitch approval required for production merge/deploy. Do not merge this PR as part of MP-01. |
