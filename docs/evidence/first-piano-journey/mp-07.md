# Evidence — MP-07

| Field | Value |
| --- | --- |
| Slice ID | MP-07 |
| Learner outcome | After Read and play is Independent on this device, a beginner can meet the left hand, read a bass-clef walk, take turns in a conversation, and share one pulse with two parts. MIDI reports pitch and time only. Hand choice is adult-observed. |
| Scope changes | Left-hand unit (L13–L16) on `/learn`. Bass and grand staff. Two-room keyboard. Hand-focus practice that keeps the current step. Unlock from L12 Independent. No buttons to L17+. Hub no longer prints a literal `null` when an omitted continue node would have been passed to `replaceChildren`. Copyright footer preserved from `main`. Marketing playground unchanged in purpose. No Next.js rewrite. No Vercel / DNS changes. Recreated on `main` @ `3da09b909fc54aaf54f0be8b00b374c3adf5cf5f`. |
| Base commit | `3da09b909fc54aaf54f0be8b00b374c3adf5cf5f` (`main`) |
| Resulting commit | *(filled after this evidence commit)* |
| Branch | `piany/mp-07-left-hand` |
| PR | https://github.com/mitchmoccia/meetpiano/pull/10 (draft into `main`) |
| Tests | `node --check` on touched JS passed. `node scripts/mp-01-check.mjs` through `mp-07-check.mjs` and `copyright-check.mjs` passed. Headless Chrome walkthrough of `/`, `/learn/`, L13–L16, and L01 one-octave guard passed. On-screen Chrome confirmed L13 two-room piano, hand-focus keep-step, hub without `null`, home Left hand link, and Xpancom copyright on `/` and `/learn`. |
| Preview URL / version | No production deploy of this branch. Local `python3 -m http.server 3000 --directory dist --bind 127.0.0.1`: `/` HTTP 200, `/learn/` HTTP 200, `/learn/?lesson=L13` HTTP 200 when L12 Independent is saved. Brand `https://meetpiano.app/learn` already HTTP 200 on current `main` (First Notes + Rhythm Club + Read and play + evidence lanes + Xpancom copyright). |
| Open risks | **Physical MIDI hardware is unverified.** Do not merge or promote production without Mitch. Not a MIDI-verified release. |
| Status | **working** (software + browser checked; hardware unchecked) |
| Next eligible slice | MP-08 leftover neighborhood or MP-11 closeout |
| Next action | Continue review. Do not merge this PR. Do not claim a MIDI-verified release. |
| Decision needed | Mitch approval required for production merge/deploy. |

## Automated fixtures (2026-09-09)

`node scripts/mp-07-check.mjs`:

- Catalog is L13–L16 only. No L17. First Notes / Rhythm Club / Read and play stay L01–L12.
- L13 locked until L12 Independent. L14 after L13 Practiced. L15 after L14 Practiced. L16 after L15 Independent.
- Four hub worlds. Left hand unlocks after Read and play.
- Bass walk / neighbors / transfer agree with fixtures (pitch, duration, clef). Conversation uses both clefs and is not Porch Steps.
- Higher C remediates on L13. Lower C is accepted. Hand focus does not leave the guided step. Fingering and hand marks are adult-observed.
- L14 independent letters off, bass clef. Higher CDE fails the bass walk.
- L15 question then answer; left practice keeps the answer step; independent uses grand staff.
- L16 dumped letters fail the two-part pulse. Hold plus walk on the clock passes. Events store pitch/time, not hand.
- Hub `replaceChildren` drops null children so the journey map does not print `null`.

`node scripts/mp-01-check.mjs` through `mp-06-check.mjs` and `copyright-check.mjs` still passed.

Product SHA: `425e18d74777d6c069b9c25e2b0c33aeda9db8c6`. Checks SHA: `6b34543de581ae39ddb639639f08366868fca37f` plus hub-null guard in `008a0ca7e07198dff8ddb491aa9b58b0483bfc55`. Docs SHA: `de9dc8d616718396177f0555c21de6a68532a58c`.

## Browser verification (2026-09-09)

Headless Chrome (`puppeteer-core` + system Chrome) against `http://127.0.0.1:3000`:

- `/` copyright is Xpancom, LLC. Learn-entry includes Left hand.
- `/learn/` with L11 Independent: four worlds, Left hand locked until Read a little tune is Independent, no L17, no hub `null` text node.
- After L12 Independent: Left hand open, L13 **Meet the left hand** Open, L14 still locked.
- `/learn/?lesson=L13`: two-room piano C3–B4. C4 remediates (`That is a C in another room. This check wants this lower C.`). C3 accepted. Right focus keeps **Name it** and says Same step.
- `/learn/?lesson=L01`: one octave C4–B4, not wide.
- L14 guided: bass staff, letters C D E.
- L15 guided: grand staff (viewBox height 230), caption Just the question.
- L16 demo: pulse clock plus Hold on bass. Walk on treble. One pulse.

On-screen Chrome:

- Hub four worlds, L13 Continue/Open, L14 Locked, footer © 2026 Xpancom, LLC, no literal `null`.
- L13 two-room piano. Wrong-room C remediates. Lower C accepted. Right hand-focus keeps the step.
- Home learn-entry: Open First Notes · Meet the keyboard (L01) · Rhythm Club · Read and play · Left hand. Footer © 2026 Xpancom, LLC.

## Hardware check (honest)

**Unverified.** This Cloud Agent environment has no attached MIDI keyboard. Do not treat on-screen keys, computer keys, or synthetic fixtures as a physical-keyboard pass.

| Check | Result |
| --- | --- |
| Browser | Google Chrome on this Linux VM |
| Web MIDI API | Present; not used with a physical instrument |
| Permission | Not granted against hardware |
| Connected inputs | None |
| Physical note-on / note-off / hold / unplug | **Not tested** |

## Unlock rules recorded in-product

| Activity | Unlocks when |
| --- | --- |
| L01–L12 First Notes + Rhythm Club + Read and play | Unchanged from MP-03 / MP-04 / MP-05 |
| L13 Meet the left hand | L12 is Independent |
| L14 Left-hand reading | L13 is Practiced or higher |
| L15 Musical conversation | L14 is Practiced or higher |
| L16 Two parts one pulse | L15 is Independent |

Help and Start over do not wipe saved evidence. Switching Left / Right / Both does not restart the lesson.
